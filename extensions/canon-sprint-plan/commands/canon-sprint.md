---
description: "Generate a sprint plan from current spec coverage"
---

# Sprint Plan

Generate a sprint plan based on Canon spec coverage gaps and team velocity.

## Arguments

$ARGUMENTS

## Instructions

Use the `sprint-plan` skill to generate a sprint plan. If arguments are provided, incorporate them as constraints (e.g., "focus on payment specs", "only 20 points this sprint", "skip infrastructure work").
