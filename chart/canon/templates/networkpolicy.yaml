{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "canon.fullname" . }}
  labels:
    {{- include "canon.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "canon.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - port: http
          protocol: TCP
  egress:
    - {}
{{- end }}
