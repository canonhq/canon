apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canon.fullname" . }}-test-connection"
  labels:
    {{- include "canon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "canon.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
