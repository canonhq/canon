apiVersion: v1
kind: Service
metadata:
  name: {{ include "canon.fullname" . }}
  labels:
    {{- include "canon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "canon.selectorLabels" . | nindent 4 }}
