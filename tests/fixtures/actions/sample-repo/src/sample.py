"""Sample module for the actions test fixture repo.

Contains realized implementations referenced by seeded-drift.md so the
audit action has something concrete to match against.
"""


def sample_feature() -> int:
    """Return a positive integer — references AC in docs/specs/seeded-drift.md."""
    return 42
